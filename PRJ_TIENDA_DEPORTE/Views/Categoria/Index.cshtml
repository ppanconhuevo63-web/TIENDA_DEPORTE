@model List<PRJ_SEMANA_03_S01.Models.Categoria>

<h2>Categoria</h2>
<a asp-action="Create" class="btn btn-primary">Nuevo</a>

<table class="table mt-3">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.idcategoria</td>
                <td>@item.nomcategoria</td>
                <td>@item.descripcion</td>
                <td>@item.estadocategoria</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.idcategoria" class="btn btn-warning btn-sm">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.idcategoria" class="btn btn-danger btn-sm" onclick="confirmarEliminar(event)">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<script>
    function confirmarEliminar(e) {
      e.preventDefault();
      const url = e.currentTarget.href;

      Swal.fire({
        title: "¿Eliminar categoría?",
        text: "Esta acción no se puede deshacer.",
        icon: "warning",
        showCancelButton: true,
        confirmButtonText: "Sí, eliminar",
        cancelButtonText: "Cancelar"
      }).then((result) => {
        if (result.isConfirmed) {
          window.location.href = url;
        }
      });
    }
</script>